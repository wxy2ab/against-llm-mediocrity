---
key: home
lang: zh
path: /zh/
title: 对抗 LLM 平庸
navTitle: 首页
kicker: 从直觉问题，到机制解释，再到实践方法
summary: 大语言模型很快就能给出流畅答案，但流畅不等于真正抓住任务价值。本项目从这个日常体验出发，解释自回归平庸的机制，并把它转化成使用、构建和协作 AI 的方法。
order: 0
heroVisual: alignment
heroPoints:
  - 当统计概率与任务价值同向上升，自回归生成可以非常强。
  - 当概率峰值偏离价值峰值，模型会生成流畅、合理、却不够有价值的答案。
  - 多数真实任务处在局部对齐区间：有些部分适合直接生成，有些部分必须被治理。
alignmentLabels:
  probability: 统计概率
  value: 任务价值
  extraordinary: 自回归卓越
  mediocre: 自回归平庸
  local: 局部对齐
  aligned: 概率与价值同向
  misaligned: 概率峰值偏离价值峰值
  partial: 部分同向，部分背离
---

## 为什么会有这个项目

你让模型做战略、诊断、设计、证明、代码或规划。它很快给出一份清楚、连贯、看起来合理的答案。你让它再改，它会更顺、更完整、更像专业稿。但真正决定价值的东西可能仍然没有出现：隐藏状态、关键约束、少见但高价值的选项、真实成功标准，或者一个更正确的问题抽象。

这就是本项目所说的 **自回归平庸**。它不是说模型没有能力，也不是说所有生成都不可靠，而是说：在某些任务里，统计上最自然的下一步，并不等于任务真正需要的下一步。模型可以一路生成得很流畅，却仍停在同一个低价值盆地里。

所以，对抗自回归平庸的关键不是简单地“写更好的提示”，而是先判断：这个任务的哪些部分已经和模型的生成倾向对齐，哪些部分只是局部对齐，哪些部分必须通过控制空间、验证、工具或人类裁决来治理。

实践目标不是压制模型的生成能力，而是保留它真正擅长的部分，同时防止局部流畅伪装成全局价值。

整站的读法也按这个判断展开：先识别流畅失败的日常形态，再解释概率与价值为什么会错开，随后把错开的部分改写成控制空间、验证对象和最小人类问题。换句话说，这不是一组松散文章，而是一条从诊断到交付的链路。

## 一句话地图

这个项目把 LLM 任务分成三个区间：

:::cards
### 自回归平庸
Tag: 概率与价值错位

模型容易生成的答案，和任务真正有价值的答案不在同一个区域。多轮润色可能提高表面质量，却没有触及真正的瓶颈。

### 自回归局部对齐
Tag: 最常见的现实区间

模型能做好很多局部操作，比如压缩、改写、列举、比较、生成结构，但这些局部成功未必能自动组合成全局成功。

### 自回归卓越
Tag: 概率与价值同向

当局部续写倾向稳定指向任务价值时，自回归不是问题，而是优势。上下文压缩、语义扩写、结构化转换和语体迁移常常属于这一区间。
:::

最常见的是中间区间。产品备忘录、代码审查、客服回复或研究综述，往往同时包含模型容易处理的部分，也包含隐藏状态、全局协调、稀缺证据或价值选择。关键不是“要不要用 AI”，而是找出对齐边界在哪里。

## 从流畅草稿走向真正有价值的答案

假设你让模型给出一份战略建议。模型很快写出了一份漂亮的备忘录，但真正应该选择哪种方案，取决于一个从未说明的预算上限。此时要求模型“再优化一下”，通常只会让文字更完整，并不会让缺失预算自动出现。

更有效的做法，是暂时停止修改最终文本，依次回答几个更窄的问题：

1. **当前处在哪个区间？** 模型处在局部对齐区间：它擅长写作和比较方案，但直接生成不能保证推荐正确。
2. **为什么局部质量不能继续预测成功？** 这里主要存在状态与规约错配：预算状态未知，主导取舍也没有明确。
3. **再次写作前，什么结构必须先显式化？** 构造一张包含成本、速度、风险和预期回报的选项矩阵。
4. **哪些变量仍不能由模型自己补足？** 先验证矩阵中的事实估计，再让人确认预算上限和主导目标。
5. **变量解决后，模型应该做什么？** 从已验证的选项矩阵和已确认的取舍中，生成最终备忘录。

这组问题形成了本项目从诊断走向交付的链条：

```text
识别概率与价值的关系区间
-> 诊断阻止全局价值的错配
-> 构造暴露缺失结构的中间对象
-> 验证证据，或获得人类治理变量
-> 从受治理状态生成最终答案
```

每一个箭头都在改变模型正在解决的任务。这条链不是每个请求都必须执行的固定流程；它的作用是避免在真正瓶颈位于上游时，仍然反复润色最终答案。

四类原始错配（primitive mismatches）用于诊断链条中的第二步：

::::cards
### Aggregation：聚合错配

局部改进不一定累积成全局价值。战略方案可以读起来很完整，却没有抓住主导取舍；代码模块可以各自合理，却破坏依赖顺序。

### Support：支持集错配

真正有价值的答案在当前模型、搜索方式和预算下很难到达。缺失的可能是少见框架、低显著性证据或非显然失败模式。

### State：状态错配

正确答案依赖隐藏或变化中的状态：市场环境、生产环境、用户情绪、权限边界、时间窗口或政策语境。

### Specification：规约错配

系统可优化的代理目标偏离真实目标。输出满足提示、风格或评分规约，却没有满足用户真正的成功条件。
::::

## 读者可以怎么进入

:::cards
### 先看直觉
Tag: 为什么重要

如果你只是想知道为什么“再让模型改一版”经常不解决问题，从“为什么重要”开始。那里解释日常使用中最常见的平庸形态。

### 看两个案例
Tag: Story Insight V4 / V6

如果你想先看完整系统，进入“案例”。案例索引会先给出阅读顺序：V4 用来理解控制空间治理，V6 用来理解高阈值下的分层治理、平台期判断和最佳状态保留。

### 再看机制
Tag: 机制

如果你关心理论本身，进入“机制”。它会把案例中的现象抽象成三种区间、策略-价值压缩，以及聚合、支持集、状态、规约四类原始错配。

### 最后看实践
Tag: 治理与协作

如果你想把框架用起来，进入“治理”和“协作”。核心方法是把困难任务拆成更适合模型的中间对象，并让人只补足 AI 无法可靠获得的关键变量。
:::

## 如何对抗 LLM 的自回归平庸

核心原则是：**保留模型已经擅长的部分，把不对齐的部分转化成更容易生成、验证和治理的任务形态。**

:::cards
### 不在最终答案空间里硬搜

困难任务不要一上来就要求“直接给最终答案”。先生成状态矩阵、评分规约、依赖图、失败模式、候选方案、查询计划或结构化提纲，让模型面对更低错配的子任务。

### 把控制知识外化

真正重要的假设、约束、边界、偏好和撤销条件，不应该埋在一段流畅文本里。它们应该变成可检查、可复用、可削弱或撤销的受治理知识对象（Governed Knowledge Object, GKO）。

### 让人治理缺失变量

人不应只是给 AI 草稿做校对。人的角色是设定价值、选择权重、授权风险、提供真实状态、判断品味，并承担责任。好的 AI agent，也就是能自主调用工具并推进任务的 AI 系统，应该问最小充分问题，而不是把整个任务交还给人。
:::

实践上，困难任务通常不应该直接进入最终文本，而应先经过一组中间对象：

```text
输入 -> 任务模型 -> 控制对象 -> 验证 / 升级 -> 最终答案
```

这些对象可以是评分规约（rubric）、状态矩阵、依赖图、候选集、失败模式清单、验证清单、GKO、受治理升级对象（Governed Escalation Object, GEO）或最小充分人类问题（Minimal Sufficient Human Query, MSHQ）。它们不是为了增加流程感，而是把高错配的最终输出问题，转化成更小、更容易让模型发挥局部优势的操作。

## 这个项目接下来会做什么

- 公共解释层：让不读论文的人也能理解自回归平庸、局部对齐和自回归卓越。
- 理论层：展开四类原始错配、知识治理、GKO/GEO 和治理循环。
- 工程层：沉淀工具与模板，把控制对象、验证循环和最小人类接入点落到真实任务中。
- 协作层：把人从普通处理者上移为价值、权限、品味、预算、证据和责任的治理者。
